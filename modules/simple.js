export default function(moduleOptions) {
  console.group("modules simple start...");
  console.log(moduleOptions);
  // console.log(moduleOptions, this.options, this.nuxt, this);
  console.groupEnd("modules simple end...");
  // this.addPlugin() // 注册插件
}
